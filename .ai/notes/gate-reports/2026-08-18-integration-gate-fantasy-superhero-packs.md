---
gate: integration
date: 2026-08-18
surface: Fantasy + Superhero art packs authored (226 briefs, 98 bios each, draft flags cleared, two new generated docs) + the Card Studio pack-switch fix, audited against the working tree over 3a4a62e
result: PASS -> the one Low fix applied
findings: 1 Low (doc wording, fixed)
---

# Integration audit: the Fantasy and Superhero packs, authored

| Rule | Verdict |
|---|---|
| §1.1 / §1.4 | N/A - no tool, registry, or shell chrome changed; the Card Studio stays the documented unregistered exception |
| §2.1 catalog/pack coverage | PASS - the coverage check now actually RUNS for both packs (draft off, early-return branch not taken): 226/226 leaf entries each, all `planned`, zero TODO; `data:validate` green |
| §2.2 generated docs | PASS - `generate-craft-art-docs.mjs --check` reports in sync; the two new docs byte-match the generator |
| §2.3 trust boundary | PASS - `isManifest` gains the `theme` check in the same change that first reads `manifest.theme`; no cast-then-use gap |
| §2.4 injection | PASS - no new primitives; the allowlist is still `theme-script.tsx` alone |
| §3.3 governance graph | PASS - only body prose changed; the new docs carry no gov:node marker and owe no edge |
| Schema ceilings | PASS - measured maxima: prompts 194/196 (cap 2000), alts 73/75 (cap 300), bios 109/97 (cap 240); zero stray bios inside grades, matching sci-fi's 98/226 split exactly |
| ILLUSTRATION_PROMPT_RULE | PASS - both new style paragraphs contain the substring; Superhero's extra comic exclusions are appended after it and cannot break the prefix match |
| Conventions | PASS |

## Applied

**Low - `docs/ARCHITECTURE.md` claimed "226 briefs and bios each".** The real
count is 226 briefs and 98 bios per pack (grades carry none). Reworded to
"226 briefs each, 98 with bios" so the inflated count does not propagate
through a doc every future audit re-reads.

## Notes

- The gate re-ran the full battery independently and confirmed green:
  data:validate, check:standards, check:security, tests 148/148, tsc clean.
- The pack-switch fix touches 6 direct `variantUrl(loadedTheme, ...)` call
  sites; `thumbnailFor` covers both the row thumbnail and the relations chip,
  so the count understates the coverage rather than overstating it.
- The gate's judgment call on whether the `isManifest` increment needed its
  own security pass was answered by running the Security gate on the same
  surface in parallel; its report is filed beside this one.
- Raised for the owner, not applied: STANDARDS §2.2 as written names only the
  `PROMPT_ROLES.md` pipeline; the `CRAFT_ART_*.md` family is generated by a
  sibling script and drift-checked by `data:validate`, so the guarantee holds
  end-to-end but the rule text does not name the second pipeline. Broaden
  §2.2 to cover both doc-generation families. (proposed amendment, needs the
  owner's consent)
