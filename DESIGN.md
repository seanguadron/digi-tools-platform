# Digi Tools Design

The canonical visual direction lives in
[`docs/DESIGN_DIRECTION.md`](docs/DESIGN_DIRECTION.md).

This file exists for tools that expect a root `DESIGN.md`. Keep it compatible
with the canonical document rather than creating a second design system.
